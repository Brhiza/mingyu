# 2026-07-05 收尾未完成事项

这份文档用于新开目标时接续，不再重复已经完成的清理和发布动作。

## 已完成

- GitHub Actions 最新 `main` 提交已通过 `lint`、`type-check`、`test`、`build` 四项检查。
- 本地已完成 `pnpm install --frozen-lockfile`、`pnpm run build`、Cloudflare Pages Functions 打包验证。
- `mingyu-core` 已抽成 workspace 包，前端不再维护一份重复的核心算法副本。
- 问题灵感保留；隐藏意图分类链路、固定词表辅助、梅花外应入口和重复提示词分类已经收掉。
- 测试策略已调整为少量关键回归加一次完整验证，后续不再铺大量细碎断言。
- `mingyu-core` 0.1.14 已发布；后续只有核心包代码继续变化时才需要再发 npm。
- Cloudflare Pages 项目的构建命令已从 `npm run build` 改为 `pnpm build`，与仓库 README 和 CI 对齐。

## 仍需另开目标处理

- Cloudflare Pages 需要等待下一次 `main` 推送后的外部检查结果；如果仍失败，优先查 Cloudflare 构建日志，不要继续盲推。
- 古籍修复算法还没有“一次性全域补齐”。已覆盖部分八字调候、格局、神煞与占法证据，但后续应按模块分批补：八字、紫微、奇门、六爻、梅花、六壬、小六壬、黄历择日、塔罗、雷诺曼、三山国王灵签、星盘。
- 粗糙算法优化不能只盯八字；需要按占法做审计，保留能解释错误修复的诗词口诀、古籍原文或明确理法依据。
- 现有测试还可以继续收敛，但只建议在修改相关模块时顺手删除明显重复、过细或只验证实现细节的断言。
- README 部署章节已对齐当前 Cloudflare 配置；如果后续 Pages 仍失败，再根据真实构建日志补充更具体的部署说明。

## 不建议继续做

- 不建议恢复隐藏分类、固定词表或让模型必须依赖本地问题分析器。
- 不建议重新在 `src/utils` 放一份 `mingyu-core` 的重复算法。
- 不建议为每个小改动继续开 PR、推多次或补大量细碎测试。
- 不建议在没有核心包代码变化时重复发布 npm。

## 建议的新目标

1. 验证 Cloudflare Pages 最新部署，若失败则基于真实构建日志修复。
2. 按占法分批补齐古籍依据和关键算法回归。
3. 继续收敛重复测试，只保留关键回归和完整验证链路。
